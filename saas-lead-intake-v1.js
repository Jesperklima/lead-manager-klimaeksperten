(()=>{
  'use strict';
  const FN_URL='https://ouqhostcsvdyrkjefiya.supabase.co/functions/v1/lead-intake-analyze';
  const STATUSES=['NY','UNDER VURDERING','KLAR TIL KONTAKT','I GANG','DIALOG','MØDE','TILBUD','AFVENTER','PÅ PAUSE','VUNDET','TABT','IKKE RELEVANT'];
  const MAX_FILES=5, MAX_FILE_BYTES=5*1024*1024, MAX_TOTAL_BYTES=9*1024*1024;
  const $=id=>document.getElementById(id);
  let selectedFiles=[];

  function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
  function isLoggedIn(){return typeof state!=='undefined'&&state?.session?.access_token&&state?.client?.id}
  function formatBytes(n){if(n<1024)return n+' B';if(n<1024*1024)return (n/1024).toFixed(0)+' KB';return (n/1024/1024).toFixed(1)+' MB'}
  function hasFiles(dt){return !!dt&&Array.from(dt.types||[]).includes('Files')}
  function setMessage(text,kind=''){const el=$('leadIntakeMessage');if(!el)return;el.className='lm-intake-message '+kind;el.textContent=text||''}
  function renderFiles(){const box=$('leadIntakeFiles');if(!box)return;box.innerHTML=selectedFiles.length?selectedFiles.map((f,i)=>`<div class="lm-intake-file"><span><strong>${esc(f.name)}</strong><small>${esc(f.type||'fil')} · ${formatBytes(f.size)}</small></span><button type="button" class="btn small" data-remove-intake-file="${i}">Fjern</button></div>`).join(''):'<div class="sub">Ingen filer valgt endnu.</div>';box.querySelectorAll('[data-remove-intake-file]').forEach(b=>b.addEventListener('click',()=>{selectedFiles.splice(Number(b.dataset.removeIntakeFile),1);renderFiles()}))}
  function acceptFiles(list){const incoming=Array.from(list||[]);if(!incoming.length)return;const next=[...selectedFiles];for(const f of incoming){if(next.length>=MAX_FILES){setMessage(`Der kan højst analyseres ${MAX_FILES} filer ad gangen.`,'warn');break}if(f.size>MAX_FILE_BYTES){setMessage(`${f.name} er for stor. Maks. ${formatBytes(MAX_FILE_BYTES)} pr. fil.`,'warn');continue}if(!next.some(x=>x.name===f.name&&x.size===f.size&&x.lastModified===f.lastModified))next.push(f)}const total=next.reduce((s,f)=>s+f.size,0);if(total>MAX_TOTAL_BYTES){setMessage(`Filerne fylder samlet for meget. Maks. ${formatBytes(MAX_TOTAL_BYTES)}.`,'warn');return}selectedFiles=next;renderFiles()}
  function ensureStyle(){if($('lmLeadIntakeCss'))return;const style=document.createElement('style');style.id='lmLeadIntakeCss';style.textContent=`
    .lm-intake-modal{width:min(760px,94vw);max-height:90vh}.lm-intake-drop{border:2px dashed var(--border);border-radius:14px;padding:24px;text-align:center;background:#fafcff;transition:.12s}.lm-intake-drop.drag{border-color:var(--accent);background:var(--soft)}.lm-intake-drop strong{display:block;font-size:16px;margin-bottom:5px}.lm-intake-files{display:grid;gap:7px;margin-top:10px}.lm-intake-file{display:flex;justify-content:space-between;align-items:center;gap:10px;border:1px solid var(--border);border-radius:10px;padding:9px 10px;background:#fff}.lm-intake-file span{min-width:0}.lm-intake-file strong,.lm-intake-file small{display:block;overflow-wrap:anywhere}.lm-intake-file small{color:var(--muted);margin-top:2px}.lm-intake-message{min-height:20px;margin-top:10px;color:var(--muted)}.lm-intake-message.ok{color:var(--ok)}.lm-intake-message.warn{color:var(--warn)}.lm-intake-message.bad{color:var(--danger)}.lm-intake-actions{display:flex;justify-content:flex-end;gap:8px;flex-wrap:wrap;margin-top:14px}.lm-file-drag-overlay{position:fixed;inset:14px;z-index:95;display:none;place-items:center;border:3px dashed var(--accent);border-radius:18px;background:rgba(246,248,252,.94);pointer-events:none;color:var(--text);font-size:18px;font-weight:800;text-align:center;padding:30px}.lm-file-drag-overlay.show{display:grid}.pipeline-col.lm-file-target{outline:3px dashed var(--accent)!important;outline-offset:-4px!important;background:var(--soft)!important}.lm-intake-progress{display:inline-flex;align-items:center;gap:7px}.lm-intake-progress:before{content:'◌';animation:lmIntakeSpin .8s linear infinite}@keyframes lmIntakeSpin{to{transform:rotate(360deg)}}@media(max-width:620px){.lm-intake-actions .btn{flex:1}.lm-intake-file{align-items:flex-start;flex-direction:column}}
  `;document.head.appendChild(style)}
  function ensureModal(){if($('leadIntakeModal'))return;const wrap=document.createElement('div');wrap.id='leadIntakeModal';wrap.className='modalback';wrap.innerHTML=`<div class="modal lm-intake-modal" role="dialog" aria-modal="true" aria-labelledby="leadIntakeTitle"><div class="drawer-head"><div><h2 id="leadIntakeTitle" style="margin:0">Opret lead fra materiale</h2><div class="sub">Træk en mail, PDF, et billede eller en note ind. Lead Manager analyserer materialet, søger efter relevante offentlige oplysninger og opretter leadet.</div></div><button class="btn" type="button" id="leadIntakeClose" aria-label="Luk">✕</button></div><div class="field"><label>Virksomhedsnavn, hvis du allerede kender det <span class="sub">(valgfrit)</span></label><input id="leadIntakeHint" autocomplete="organization" placeholder="Fx Virksomhed A/S"></div><div class="field"><label>Mailtekst / note / ekstra oplysninger <span class="sub">(valgfrit)</span></label><textarea id="leadIntakeNote" rows="5" placeholder="Indsæt fx en mail, telefonnotat eller de oplysninger du har fået."></textarea></div><div class="field"><label>Filer</label><div class="lm-intake-drop" id="leadIntakeDrop"><strong>Slip filer her</strong><div class="sub">PDF, billeder, Word, tekst og EML · maks. 5 filer</div><button type="button" class="btn" id="leadIntakeChoose" style="margin-top:10px">Vælg fil</button><input id="leadIntakeInput" type="file" multiple hidden></div><div class="lm-intake-files" id="leadIntakeFiles"></div></div><div class="field"><label>Placér lead i pipeline</label><select id="leadIntakeStatus">${STATUSES.map(s=>`<option>${esc(s)}</option>`).join('')}</select></div><div class="lm-intake-message" id="leadIntakeMessage" aria-live="polite"></div><div class="lm-intake-actions"><button class="btn" type="button" id="leadIntakeCancel">Annuller</button><button class="btn primary" type="button" id="leadIntakeCreate">Analyser og opret lead</button></div></div>`;document.body.appendChild(wrap);const input=$('leadIntakeInput'),drop=$('leadIntakeDrop');$('leadIntakeChoose').onclick=()=>input.click();input.onchange=()=>{acceptFiles(input.files);input.value=''};['dragenter','dragover'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',e=>acceptFiles(e.dataTransfer.files));$('leadIntakeClose').onclick=closeModal;$('leadIntakeCancel').onclick=closeModal;wrap.addEventListener('click',e=>{if(e.target===wrap)closeModal()});$('leadIntakeCreate').onclick=submit;renderFiles()}
  function ensureOverlay(){if($('lmFileDragOverlay'))return;const d=document.createElement('div');d.id='lmFileDragOverlay';d.className='lm-file-drag-overlay';d.innerHTML='<div>Slip materialet i Lead Manager<br><span class="sub">Du kan også slippe direkte på en kolonne i pipelinen</span></div>';document.body.appendChild(d)}
  function openModal(status='NY',files=[]){ensureStyle();ensureModal();selectedFiles=[];acceptFiles(files);$('leadIntakeStatus').value=STATUSES.includes(status)?status:'NY';setMessage('');$('leadIntakeModal').classList.add('open');setTimeout(()=>($('leadIntakeHint')||$('leadIntakeNote'))?.focus(),30)}
  function closeModal(){const m=$('leadIntakeModal');if(m)m.classList.remove('open');selectedFiles=[];renderFiles();setMessage('');const n=$('leadIntakeNote'),h=$('leadIntakeHint');if(n)n.value='';if(h)h.value=''}
  function readAsBase64(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=()=>reject(r.error||new Error('Fil kunne ikke læses'));r.onload=()=>{const s=String(r.result||''),i=s.indexOf(',');resolve(i>=0?s.slice(i+1):s)};r.readAsDataURL(file)})}
  async function submit(){if(!isLoggedIn()){setMessage('Du skal være logget ind for at oprette et lead.','bad');return}const note=$('leadIntakeNote').value.trim(),hint=$('leadIntakeHint').value.trim();if(!selectedFiles.length&&!note&&!hint){setMessage('Tilføj mindst en fil, en note eller et virksomhedsnavn.','warn');return}const btn=$('leadIntakeCreate');btn.disabled=true;btn.innerHTML='<span class="lm-intake-progress">Analyserer og søger på nettet…</span>';setMessage('Lead Manager udtrækker oplysninger og kontrollerer virksomheden mod offentlige kilder.');try{const files=[];for(const f of selectedFiles)files.push({name:f.name,type:f.type||'',size:f.size,data:await readAsBase64(f)});const res=await fetch(FN_URL,{method:'POST',headers:{Authorization:'Bearer '+state.session.access_token,'Content-Type':'application/json'},body:JSON.stringify({client_id:state.client.id,target_status:$('leadIntakeStatus').value,note,company_hint:hint,files})});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||('HTTP '+res.status));if(data.needs_review){setMessage(data.message||'Virksomheden kunne ikke identificeres sikkert. Tilføj virksomhedsnavn og prøv igen.','warn');return}setMessage(data.existing_lead?'Materialet er lagt på det eksisterende lead.':'Leadet er oprettet og research er sat i gang.','ok');if(typeof loadAll==='function')await loadAll();setTimeout(()=>{closeModal();document.querySelector('.nav button[data-view="pipeline"]')?.click();if(data.lead_id&&typeof openLead==='function')setTimeout(()=>openLead(data.lead_id),120);if(typeof toast==='function')toast(data.existing_lead?'Eksisterende lead opdateret med nyt materiale':'Lead oprettet fra materiale')},450)}catch(err){setMessage('Kunne ikke oprette lead: '+(err?.message||err),'bad')}finally{btn.disabled=false;btn.textContent='Analyser og opret lead'}}
  function installButton(){if(!isLoggedIn()||$('leadIntakeBtn'))return;const newLead=$('newLead');if(newLead){const b=document.createElement('button');b.id='leadIntakeBtn';b.type='button';b.className='btn';b.textContent='⇩ Lead fra fil';b.title='Opret lead fra mail, note, billede eller dokument';b.addEventListener('click',()=>openModal('NY'));newLead.insertAdjacentElement('afterend',b);return}const actions=document.querySelector('.pipeline-actions');if(actions){const b=document.createElement('button');b.id='leadIntakeBtn';b.type='button';b.className='btn primary';b.textContent='⇩ Lead fra fil';b.addEventListener('click',()=>openModal('NY'));actions.appendChild(b)}}
  function clearTargets(){document.querySelectorAll('.pipeline-col.lm-file-target').forEach(x=>x.classList.remove('lm-file-target'))}
  function globalDragHandlers(){let depth=0;document.addEventListener('dragenter',e=>{if(!hasFiles(e.dataTransfer))return;depth++;ensureOverlay();$('lmFileDragOverlay')?.classList.add('show')},true);document.addEventListener('dragover',e=>{if(!hasFiles(e.dataTransfer))return;e.preventDefault();e.dataTransfer.dropEffect='copy';clearTargets();const col=e.target?.closest?.('.pipeline-col[data-pipeline-status]');if(col)col.classList.add('lm-file-target')},true);document.addEventListener('dragleave',e=>{if(!hasFiles(e.dataTransfer))return;depth=Math.max(0,depth-1);if(depth===0){$('lmFileDragOverlay')?.classList.remove('show');clearTargets()}},true);document.addEventListener('drop',e=>{if(!hasFiles(e.dataTransfer))return;e.preventDefault();depth=0;$('lmFileDragOverlay')?.classList.remove('show');const col=e.target?.closest?.('.pipeline-col[data-pipeline-status]');const status=col?.dataset?.pipelineStatus||'NY';clearTargets();openModal(status,e.dataTransfer.files)},true)}
  function init(){ensureStyle();ensureModal();ensureOverlay();installButton();setInterval(installButton,1400)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();globalDragHandlers();
})();
