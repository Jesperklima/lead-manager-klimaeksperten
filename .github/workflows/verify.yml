name: Verify Lead Manager baseline

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify canonical bytes, policy markers and Vercel config
        run: bash scripts/verify-repository-baseline.sh
